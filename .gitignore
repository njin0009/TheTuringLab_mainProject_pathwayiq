# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Build outputs ──
.next/
out/
dist/
build/
.serverless/

# ── Environment variables (NEVER commit these) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Data pipeline — raw source files (too large, may contain PII) ──
data/raw/

# ── AWS / Cloudflare credentials ──
.aws/
.wrangler/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Logs ──
*.log
npm-debug.log*

# ── TypeScript ──
*.tsbuildinfo

# ── Test coverage ──
coverage/
.nyc_output/

# ── Python ──
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyi

# ── Data pipeline — processed & output files ──
data/pathwayiq-Data Pipeline/pipeline/logs/
data/pathwayiq-Data Pipeline/pipeline/profiles/
data/pathwayiq-Data Pipeline/pipeline/silver/
data/pathwayiq-Data Pipeline/cleaned_data/
*.parquet

# ── Raw data files ──
*.xlsx
*.csv
~$*

# Raw data files — do not commit large or sensitive source files
src/backend/ai_risk/data/raw/

# Allow processed ai_risk CSV (committed intentionally)
!src/backend/ai_risk/data/processed/*.csv
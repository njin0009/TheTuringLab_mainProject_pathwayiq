# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Build outputs ──
.next/
out/
dist/
build/
.serverless/

# ── Environment variables (NEVER commit these) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Data pipeline — raw source files (too large, may contain PII) ──
data/raw/

# ── AWS / Cloudflare credentials ──
.aws/
.wrangler/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Logs ──
*.log
npm-debug.log*

# ── TypeScript ──
*.tsbuildinfo

# ── Test coverage ──
coverage/
.nyc_output/

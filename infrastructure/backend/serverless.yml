service: pathwayiq-backend

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-southeast-2

functions:
  search:
    handler: src/search.handler
    events:
      - httpApi:
          path: /careers
          method: GET